-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.break_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  trip_id uuid,
  driver_id uuid,
  start_time timestamp with time zone,
  end_time timestamp with time zone,
  duration_minutes integer,
  lat numeric,
  lng numeric,
  break_type text,
  notes text,
  CONSTRAINT break_logs_pkey PRIMARY KEY (id),
  CONSTRAINT break_logs_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id),
  CONSTRAINT break_logs_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.users(id)
);
CREATE TABLE public.crash_checklists (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  driver_id text NOT NULL,
  trip_id text,
  vehicle_id text NOT NULL,
  items jsonb NOT NULL DEFAULT '[]'::jsonb,
  completed_at timestamp with time zone,
  latitude double precision,
  longitude double precision,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT crash_checklists_pkey PRIMARY KEY (id)
);
CREATE TABLE public.defects (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  vehicle_id uuid NOT NULL,
  reported_by uuid,
  work_order_id uuid,
  title text NOT NULL,
  description text,
  category text CHECK (category = ANY (ARRAY['engine'::text, 'brakes'::text, 'tires'::text, 'electrical'::text, 'body_damage'::text, 'other'::text])),
  priority text DEFAULT 'medium'::text CHECK (priority = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text, 'critical'::text])),
  status text DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'in_progress'::text, 'resolved'::text, 'closed'::text])),
  reported_at timestamp with time zone DEFAULT now(),
  resolved_at timestamp with time zone,
  trip_id uuid,
  image_urls jsonb,
  CONSTRAINT defects_pkey PRIMARY KEY (id),
  CONSTRAINT defects_work_order_id_fkey FOREIGN KEY (work_order_id) REFERENCES public.maintenance_work_orders(id),
  CONSTRAINT defects_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT defects_reported_by_fkey FOREIGN KEY (reported_by) REFERENCES public.users(id),
  CONSTRAINT defects_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id)
);
CREATE TABLE public.driver_vehicle_assignments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  driver_id uuid NOT NULL,
  vehicle_id uuid NOT NULL,
  shift_start timestamp with time zone NOT NULL,
  shift_end timestamp with time zone NOT NULL,
  status text DEFAULT 'scheduled'::text CHECK (status = ANY (ARRAY['scheduled'::text, 'completed'::text, 'cancelled'::text])),
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT driver_vehicle_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT driver_vehicle_assignments_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.users(id),
  CONSTRAINT driver_vehicle_assignments_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT driver_vehicle_assignments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.fuel_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  trip_id uuid,
  driver_id uuid,
  fuel_station text,
  amount_paid numeric,
  fuel_volume numeric,
  receipt_image_url text,
  logged_at timestamp with time zone DEFAULT now(),
  CONSTRAINT fuel_logs_pkey PRIMARY KEY (id),
  CONSTRAINT fuel_logs_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id),
  CONSTRAINT fuel_logs_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.users(id)
);
CREATE TABLE public.geofences (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text,
  center_lat numeric,
  center_lng numeric,
  radius_meters integer,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT geofences_pkey PRIMARY KEY (id),
  CONSTRAINT geofences_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.incidents (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  trip_id uuid,
  vehicle_id uuid,
  driver_id uuid,
  severity text CHECK (severity = ANY (ARRAY['possible_crash'::text, 'hard_brake'::text])),
  lat numeric,
  lng numeric,
  speed_before numeric,
  speed_after numeric,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT incidents_pkey PRIMARY KEY (id),
  CONSTRAINT incidents_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id),
  CONSTRAINT incidents_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT incidents_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.users(id)
);
CREATE TABLE public.maintenance_parts_used (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  work_order_id uuid,
  part_id uuid,
  quantity integer,
  cost numeric,
  CONSTRAINT maintenance_parts_used_pkey PRIMARY KEY (id),
  CONSTRAINT maintenance_parts_used_work_order_id_fkey FOREIGN KEY (work_order_id) REFERENCES public.maintenance_work_orders(id),
  CONSTRAINT maintenance_parts_used_part_id_fkey FOREIGN KEY (part_id) REFERENCES public.parts_inventory(id)
);
CREATE TABLE public.maintenance_work_orders (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  vehicle_id uuid,
  created_by uuid,
  assigned_to uuid,
  description text,
  priority text CHECK (priority = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text])),
  status text DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'in_progress'::text, 'completed'::text])),
  estimated_cost numeric,
  created_at timestamp with time zone DEFAULT now(),
  completed_at timestamp with time zone,
  service_odometer double precision,
  scheduled_date timestamp with time zone,
  CONSTRAINT maintenance_work_orders_pkey PRIMARY KEY (id),
  CONSTRAINT maintenance_work_orders_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT maintenance_work_orders_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT maintenance_work_orders_assigned_to_fkey FOREIGN KEY (assigned_to) REFERENCES public.users(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  recipient_id uuid,
  type text CHECK (type = ANY (ARRAY['document_expiry'::text, 'geofence_entry'::text, 'geofence_exit'::text, 'maintenance_due'::text, 'crash_alert'::text, 'break_violation'::text])),
  vehicle_id uuid,
  trip_id uuid,
  message text,
  is_read boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_recipient_id_fkey FOREIGN KEY (recipient_id) REFERENCES public.users(id),
  CONSTRAINT notifications_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT notifications_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id)
);
CREATE TABLE public.orders (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_number text UNIQUE,
  customer_name text NOT NULL,
  customer_phone text,
  customer_email text,
  total_weight_kg numeric NOT NULL,
  total_packages integer,
  cargo_type text CHECK (cargo_type = ANY (ARRAY['general'::text, 'fragile'::text, 'perishable'::text, 'hazardous'::text, 'oversized'::text])),
  special_instructions text,
  origin_name text,
  origin_lat numeric,
  origin_lng numeric,
  destination_name text,
  destination_lat numeric,
  destination_lng numeric,
  requested_pickup_at timestamp with time zone,
  requested_delivery_at timestamp with time zone,
  status text DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'confirmed'::text, 'dispatched'::text, 'in_transit'::text, 'delivered'::text, 'cancelled'::text])),
  priority text DEFAULT 'normal'::text CHECK (priority = ANY (ARRAY['low'::text, 'normal'::text, 'high'::text, 'urgent'::text])),
  quoted_price numeric,
  final_price numeric,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  amount_received numeric DEFAULT 0,
  payment_status text DEFAULT 'unpaid'::text CHECK (payment_status = ANY (ARRAY['unpaid'::text, 'partial'::text, 'paid'::text])),
  payment_method text CHECK (payment_method = ANY (ARRAY['cash'::text, 'bank_transfer'::text, 'cheque'::text, 'upi'::text])),
  invoice_number text UNIQUE,
  waypoints jsonb,
  trip_id uuid,
  assigned_driver_id uuid,
  assigned_vehicle_id uuid,
  CONSTRAINT orders_pkey PRIMARY KEY (id),
  CONSTRAINT orders_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT orders_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id),
  CONSTRAINT orders_assigned_driver_id_fkey FOREIGN KEY (assigned_driver_id) REFERENCES public.users(id),
  CONSTRAINT orders_assigned_vehicle_id_fkey FOREIGN KEY (assigned_vehicle_id) REFERENCES public.vehicles(id)
);
CREATE TABLE public.parts_inventory (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  stock integer DEFAULT 0,
  threshold integer DEFAULT 5,
  unit_cost numeric,
  last_updated timestamp with time zone DEFAULT now(),
  CONSTRAINT parts_inventory_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sos_alerts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  driver_id text NOT NULL,
  vehicle_id text NOT NULL,
  trip_id text,
  latitude double precision NOT NULL DEFAULT 0,
  longitude double precision NOT NULL DEFAULT 0,
  speed double precision,
  timestamp timestamp with time zone NOT NULL DEFAULT now(),
  status text NOT NULL DEFAULT 'active'::text,
  CONSTRAINT sos_alerts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sos_location_pings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  sos_alert_id text,
  driver_id text NOT NULL,
  latitude double precision NOT NULL,
  longitude double precision NOT NULL,
  speed double precision,
  timestamp timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT sos_location_pings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.trip_gps_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  trip_id uuid NOT NULL,
  lat numeric NOT NULL,
  lng numeric NOT NULL,
  speed numeric,
  heading numeric,
  recorded_at timestamp with time zone DEFAULT now(),
  CONSTRAINT trip_gps_logs_pkey PRIMARY KEY (id),
  CONSTRAINT trip_gps_logs_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id)
);
CREATE TABLE public.trip_stops (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  trip_id uuid NOT NULL,
  name text,
  lat numeric,
  lng numeric,
  stop_type text CHECK (stop_type = ANY (ARRAY['pickup'::text, 'delivery'::text])),
  goods_description text,
  packages integer,
  weight_kg numeric,
  receiver_name text,
  signature_url text,
  photo_url text,
  delivered_at timestamp with time zone,
  sequence_number integer NOT NULL,
  CONSTRAINT trip_stops_pkey PRIMARY KEY (id),
  CONSTRAINT trip_stops_trip_id_fkey FOREIGN KEY (trip_id) REFERENCES public.trips(id)
);
CREATE TABLE public.trips (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  vehicle_id uuid,
  driver_id uuid,
  assignment_id uuid,
  shipment_description text,
  shipment_weight_kg numeric,
  shipment_package_count integer,
  fragile boolean DEFAULT false,
  special_instructions text,
  start_lat numeric,
  start_lng numeric,
  start_name text,
  end_lat numeric,
  end_lng numeric,
  end_name text,
  distance_km numeric,
  estimated_duration_minutes integer,
  actual_duration_minutes integer,
  fuel_used_liters numeric,
  status text DEFAULT 'scheduled'::text CHECK (status = ANY (ARRAY['scheduled'::text, 'active'::text, 'completed'::text, 'cancelled'::text])),
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  start_time timestamp with time zone,
  end_time timestamp with time zone,
  order_id uuid,
  CONSTRAINT trips_pkey PRIMARY KEY (id),
  CONSTRAINT trips_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT trips_assignment_id_fkey FOREIGN KEY (assignment_id) REFERENCES public.driver_vehicle_assignments(id),
  CONSTRAINT trips_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT trips_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT trips_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL,
  name text NOT NULL,
  email text,
  phone text,
  role text NOT NULL CHECK (role = ANY (ARRAY['manager'::text, 'driver'::text, 'maintenance'::text])),
  license_number text,
  license_expiry date,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  last_login timestamp with time zone,
  profile_picture_url text,
  employee_id text UNIQUE,
  map_preference text NOT NULL DEFAULT 'standard'::text CHECK (map_preference = ANY (ARRAY['standard'::text, 'satellite'::text])),
  units text NOT NULL DEFAULT 'km'::text CHECK (units = ANY (ARRAY['km'::text, 'miles'::text])),
  two_factor_enabled boolean NOT NULL DEFAULT false,
  is_deleted boolean NOT NULL DEFAULT false,
  employment_status text DEFAULT 'active'::text CHECK (employment_status = ANY (ARRAY['active'::text, 'inactive'::text, 'suspended'::text])),
  operational_status text DEFAULT 'off_duty'::text CHECK (operational_status = ANY (ARRAY['off_duty'::text, 'available'::text, 'on_trip'::text])),
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id),
  CONSTRAINT users_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.vehicle_documents (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  vehicle_id uuid NOT NULL,
  document_type text CHECK (document_type = ANY (ARRAY['insurance'::text, 'registration'::text, 'permit'::text, 'pollution'::text, 'fitness'::text])),
  file_url text NOT NULL,
  expiry_date date,
  uploaded_by uuid,
  uploaded_at timestamp with time zone DEFAULT now(),
  metadata jsonb,
  CONSTRAINT vehicle_documents_pkey PRIMARY KEY (id),
  CONSTRAINT vehicle_documents_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT vehicle_documents_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES public.users(id)
);
CREATE TABLE public.vehicle_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  vehicle_id text NOT NULL,
  trip_id text,
  event_type text NOT NULL,
  timestamp timestamp with time zone DEFAULT now(),
  CONSTRAINT vehicle_events_pkey PRIMARY KEY (id)
);
CREATE TABLE public.vehicle_inspections (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  vehicle_id uuid,
  driver_id uuid,
  inspection_type text CHECK (inspection_type = ANY (ARRAY['pre_trip'::text, 'post_trip'::text])),
  brakes_ok boolean,
  tires_ok boolean,
  headlights_ok boolean,
  mirrors_ok boolean,
  engine_ok boolean,
  issues_reported text,
  image_urls jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT vehicle_inspections_pkey PRIMARY KEY (id),
  CONSTRAINT vehicle_inspections_vehicle_id_fkey FOREIGN KEY (vehicle_id) REFERENCES public.vehicles(id),
  CONSTRAINT vehicle_inspections_driver_id_fkey FOREIGN KEY (driver_id) REFERENCES public.users(id)
);
CREATE TABLE public.vehicles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  plate_number text NOT NULL UNIQUE,
  chassis_number text,
  manufacturer text,
  model text,
  fuel_type text CHECK (fuel_type = ANY (ARRAY['diesel'::text, 'petrol'::text, 'electric'::text])),
  fuel_tank_capacity numeric,
  carrying_capacity numeric,
  purchase_date date,
  odometer numeric DEFAULT 0,
  status text DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'maintenance'::text, 'inactive'::text])),
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  last_service_date timestamp with time zone,
  last_service_odometer double precision,
  service_interval_km double precision,
  service_interval_months integer,
  maintenance_notes text,
  CONSTRAINT vehicles_pkey PRIMARY KEY (id),
  CONSTRAINT vehicles_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);